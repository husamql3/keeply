Your goul is to update any vulnerable dependecies.

Do the following:

1. Run `bun audit` to find vulnerable installed packages in this project
2. Run `bun update` to apply updates
3. Run tests and verify the updates didn't break anything
