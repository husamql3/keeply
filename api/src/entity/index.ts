import { MagicLinkToken } from "./magic-link-token.entity";
import { RefreshToken } from "./refresh-token.entity";
import { User } from "./user.entity";

export const entities = [RefreshToken, User, MagicLinkToken];
